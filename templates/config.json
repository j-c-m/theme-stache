{
  "shell.sh": {
    "directory": "shell",
    "filename": "{{theme-source-slug}}-{{theme-slug}}.sh"
  },
  "alacritty.toml": {
    "directory": "alacritty",
    "filename": "{{theme-source-slug}}-{{theme-slug}}.toml"
  },
  "json.json": {
    "directory": "json",
    "filename": "{{theme-source-slug}}-{{theme-slug}}.json"
  },
  "iterm.itermcolors": {
    "directory": "itermcolors",
    "filename": "{{theme-source-slug}}-{{theme-slug}}.itermcolors"
  },
  "ghostty": {
    "directory": "ghostty",
    "filename": "{{theme-source-slug}}-{{theme-slug}}"
  }
}
